# 安全说明

Page Agent 可以读取和修改网页、访问 Cookie、发起网络请求、控制下载和代理，并把页面内容发送给用户配置的模型服务。这些能力是产品功能的一部分，也意味着错误的 Flow 或连接配置可能造成数据泄露或页面误操作。

## 使用建议

- 只从可信来源加载扩展，并在 Chrome/Edge 的扩展管理页审阅权限。
- 不要在 issue、日志、录制、Flow 导出或截图中提交 API Key、Cookie、密码、验证码、个人信息或真实业务数据。
- MCP Server 默认只绑定本机回环地址；需要改变监听地址时，必须自行配置网络访问控制和认证层。
- 运行外部 Flow 前先检查目标 URL、请求头、脚本和写入动作。

## 报告漏洞

请使用 GitHub Security Advisories 的私密漏洞报告入口提交安全问题，并提供受影响版本、复现步骤、影响范围和修复建议。不要在公开 issue 中发布可直接利用的凭据或完整漏洞细节。
