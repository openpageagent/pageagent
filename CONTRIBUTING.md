# 贡献指南

感谢贡献 Page Agent。提交改动前，请确认它解决一个明确问题，并保持现有的 Manifest V3、ARP/1 和资源权限边界。

## 工作方式

1. 从当前默认分支创建功能分支。
2. 只提交与问题直接相关的文件，不提交 `node_modules/`、`dist/`、日志、密钥或本机 IDE 文件。
3. 新增用户可见行为时同步更新 `README.md` 或对应规范文档。
4. 涉及依赖版本时同时更新对应 lockfile，并确认第三方许可证声明仍准确。

## 提交前检查

本项目不要求构建产物进入仓库。提交前至少完成：

```bash
git diff --check
node --check <已修改的 JavaScript 文件>
```

需要发布 ZIP 时，再按 README 的打包说明安装工具并运行打包脚本。请在合并请求中说明变更范围、手工验证步骤和已知限制。

## 代码约定

- 优先使用现有模块和数据契约，避免为单一调用新增抽象。
- 保留跨文件全局名、消息字段、资源 URI 和 Flow schema 的兼容性。
- 不把 API Key、Cookie、录制内容或真实业务数据写入源码、日志、截图和示例。
- 用户可见文本和文档默认使用简体中文；协议字段、代码和命令保持原样。
